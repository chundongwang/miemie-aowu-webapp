# Claude Code Instructions

## Commit & push policy

After every completed feature or bug fix, **always** commit and push before moving on:

```
git add -A
git commit -m "..."
git push
```

Do not batch multiple features into one commit unless they are tightly coupled. One logical change = one commit.
